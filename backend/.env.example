# Backend environment variables
# Copy this file to .env and adjust as needed.

# --- Core ---
NODE_ENV=development
HOST=0.0.0.0
PORT=3000

# --- PostgreSQL connection ---
DATABASE_URL=postgresql://orga:orga@localhost:5432/orga?schema=public

# --- JWT secrets ---
# Development defaults below; in production you MUST set strong secrets.
JWT_ACCESS_SECRET=access-secret
JWT_REFRESH_SECRET=refresh-secret
# Token TTLs (seconds)
JWT_ACCESS_EXPIRES_IN=900
JWT_REFRESH_EXPIRES_IN=604800

# --- CORS ---
# In production, set comma-separated list of allowed origins
# Example: https://app.example.com,https://admin.example.com
ALLOWED_ORIGINS=

# --- Swagger ---
# By default Swagger is enabled only when NODE_ENV!="production".
# Set to true to enable in production (not recommended publicly).
SWAGGER_ENABLED=false
SWAGGER_PATH=docs

# --- Rate limiting ---
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=300
RATE_LIMIT_AUTH_MAX=100
# Extra for /admin
RATE_LIMIT_ADMIN_MAX=50
# Set to a number (e.g., 1) to trust that many proxies (if behind nginx)
TRUST_PROXY=

# Optional: API base URL used in Swagger and frontend default
API_BASE_URL=http://localhost:3000

# --- Admin (dev/local convenience) ---
# Credentials for the seeded admin user created by `npm run seed`
ADMIN_EMAIL=admin@orga.app
ADMIN_PASSWORD=admin1234
# Optional IP allowlist for /admin (comma-separated, e.g., 127.0.0.1,::1)
ADMIN_ALLOWLIST=
